apiVersion: v1
kind: Service
metadata:
  name: {{ include "neuralcore.fullname" . }}
  labels:
    {{- include "neuralcore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "neuralcore.selectorLabels" . | nindent 4 }}
